version: "3.1"

# The duplicate direct-entry override flow was intentionally removed.
# repeat_compensation_closure in chipotle_support.yml is the sole canonical
# flow for requests to override a prior repeat-compensation denial.
flows: {}
